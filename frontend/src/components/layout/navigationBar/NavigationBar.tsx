import { ReactElement, ReactNode } from "react";

const NavigationBar= () => {
    return (
        <div id="navigationBarContainer">
            <h1>  any thing that we can write </h1>
        </div>
    );
}

export default NavigationBar;